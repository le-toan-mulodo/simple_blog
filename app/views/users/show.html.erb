<% provide(:title, @user.name) %>
<div class="row">
	<aside class="span8" >
		<section>
			<h1> <%= gravatar_for @user %>
			<%= @user.name %> </h1>

			<%= @user.email %>
			<p>
				<%= @user.address %>
			</p>
		</section>
	</aside>
</div>